import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const hike = () => {
  return (
    <View>
      <Text>hike</Text>
    </View>
  );
};

export default hike;

const styles = StyleSheet.create({});
